import 'package:flutter/material.dart';

class SelectStandInPage extends StatelessWidget {
  const SelectStandInPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Select Stand-In'),
      ),
      body: const Center(
        child: Text('Select Stand-In Page'),
      ),
    );
  }
}
